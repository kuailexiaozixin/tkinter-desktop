# win32pipe 常量（共 16 个）

> 来源 https://mhammond.github.io/pywin32/win32pipe_<CONST>.html

- **FILE_FLAG_FIRST_PIPE_INSTANCE** — --- ## FILE_FLAG_FIRST_PIPE_INSTANCE const win32pipe.FILE_FLAG_FIRST_PIPE_INSTANCE;
- **NMPWAIT_NOWAIT** — --- ## NMPWAIT_NOWAIT const win32pipe.NMPWAIT_NOWAIT;
- **NMPWAIT_USE_DEFAULT_WAIT** — --- ## NMPWAIT_USE_DEFAULT_WAIT const win32pipe.NMPWAIT_USE_DEFAULT_WAIT;
- **NMPWAIT_WAIT_FOREVER** — --- ## NMPWAIT_WAIT_FOREVER const win32pipe.NMPWAIT_WAIT_FOREVER;
- **PIPE_ACCEPT_REMOTE_CLIENTS** — --- ## PIPE_ACCEPT_REMOTE_CLIENTS const win32pipe.PIPE_ACCEPT_REMOTE_CLIENTS;
- **PIPE_ACCESS_DUPLEX** — --- ## PIPE_ACCESS_DUPLEX const win32pipe.PIPE_ACCESS_DUPLEX;
- **PIPE_ACCESS_INBOUND** — --- ## PIPE_ACCESS_INBOUND const win32pipe.PIPE_ACCESS_INBOUND;
- **PIPE_ACCESS_OUTBOUND** — --- ## PIPE_ACCESS_OUTBOUND const win32pipe.PIPE_ACCESS_OUTBOUND;
- **PIPE_NOWAIT** — --- ## PIPE_NOWAIT const win32pipe.PIPE_NOWAIT;
- **PIPE_READMODE_BYTE** — --- ## PIPE_READMODE_BYTE const win32pipe.PIPE_READMODE_BYTE;
- **PIPE_READMODE_MESSAGE** — --- ## PIPE_READMODE_MESSAGE const win32pipe.PIPE_READMODE_MESSAGE;
- **PIPE_REJECT_REMOTE_CLIENTS** — --- ## PIPE_REJECT_REMOTE_CLIENTS const win32pipe.PIPE_REJECT_REMOTE_CLIENTS;
- **PIPE_TYPE_BYTE** — --- ## PIPE_TYPE_BYTE const win32pipe.PIPE_TYPE_BYTE;
- **PIPE_TYPE_MESSAGE** — --- ## PIPE_TYPE_MESSAGE const win32pipe.PIPE_TYPE_MESSAGE;
- **PIPE_UNLIMITED_INSTANCES** — --- ## PIPE_UNLIMITED_INSTANCES const win32pipe.PIPE_UNLIMITED_INSTANCES;
- **PIPE_WAIT** — --- ## PIPE_WAIT const win32pipe.PIPE_WAIT;
